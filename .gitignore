# Python
__pycache__/
*.py[cod]
.venv/
venv/

# Secrets (never commit keys or app passwords)
.env

# Local-only artifacts
output/
data/cron.log
data/launchd.log
data/seen.db

# Transient pipeline files (regenerated every run — not memory)
data/pending.json
data/analysis.json

# NOTE: committed on purpose (cloud agent's cross-day memory + published site):
#   data/seen.json  data/story_memory.json  data/feedback.json  docs/
